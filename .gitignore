# Dependencies
node_modules/

# Secrets & env
.env
.env.*
!.env.example

# Build output
build/
dist/
.cache/
public/build/

# Prisma
prisma/*.db
prisma/*.db-journal

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
.idea/
.vscode/

# Test / coverage
coverage/
playwright-report/
test-results/

# Shopify
.shopify/

# TEMP: CI workflow held out of git until a token with GitHub `workflow` scope
# is available (see PROGRESS.md). Remove this line + `git add .github/workflows/ci.yml`
# once `gh auth refresh -s workflow` is done.
.github/workflows/ci.yml
